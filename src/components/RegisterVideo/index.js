import { StyledRegisterVideo } from "./styles";

export default function RegisterVideo() {
    // falta adicionar o floating button
    // escurecer o fundo quando abre o register video (modal)
    // -> precisamos controlar o state
    // -> formulário do register video
    return (
        <StyledRegisterVideo>
            {/* Chamada do RegisterVideo */}
        </StyledRegisterVideo>
    )
}